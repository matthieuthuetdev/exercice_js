async function pars_json(url) {
    const promesse = new Promise(reussi,echouer,)
    const reponse = await fetch(url)
    return fichier = reponse.json()
}
    const reponse = pars_json("https://arfp.github.io/tp/web/javascript/03-cardgame/cardgame.json")
    console.log(reponse.ok)